[package]
name = "jexpr"
version = "0.1.0"
edition = "2024"

[dependencies]
nom = "8.0.0"

[dev-dependencies]
criterion = { version = "0.8", features = ["html_reports"] }

[[bench]]
name = "parser"
harness = false
